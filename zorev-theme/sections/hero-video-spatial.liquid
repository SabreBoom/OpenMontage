{%- comment -%}
  Deprecated. Orphaned since v10 (no template referenced it), and its
  defaults were the abandoned #050505 / #D4AF37 palette. It also loaded
  model-viewer (~1MB, bundles three.js) and depended on GSAP + ScrollTrigger,
  neither of which this theme loads.

  This file is intentionally empty. `themeFilesDelete` is blocked by the MCP
  safety policy; delete the file itself from Shopify admin > Code editor to
  remove it from the section picker entirely.
{%- endcomment -%}
{% schema %}
{ "name": "3D hero (old)", "tag": "section", "settings": [] }
{% endschema %}
